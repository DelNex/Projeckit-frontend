# Local frontend settings for Project KIT
# Use this file as a template and copy it to .env with your actual values.
# Example production backend URL: https://your-backend-domain.com
# Set this to the deployed backend hostname when using the frontend in production.
API_BASE_URL=http://localhost:4000
